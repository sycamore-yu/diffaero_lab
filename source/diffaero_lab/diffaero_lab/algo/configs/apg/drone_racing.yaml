# @package hydra_configs
# Target the algorithm config group
_target_: diffaero_lab.algo.algorithms.apg.APGConfig

# APG algorithm hyperparameters
lr: 3e-4
max_grad_norm: 1.0
rollout_horizon: 32

# Network architecture
hidden_dims: [256, 128, 64]
